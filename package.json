{
  "name": "code-forge-ai-root",
  "private": true,
  "scripts": {
    "build": "npx pnpm --dir ./Code-Forge-AI install --frozen-lockfile --prefer-offline && npx pnpm --dir ./Code-Forge-AI run build",
    "start": "npx pnpm --dir ./Code-Forge-AI run start"
  }
}
