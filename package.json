{
  "name": "workspace",
  "version": "0.0.0",
  "packageManager": "pnpm@11.10.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const fs=require('fs'); ['package-lock.json','yarn.lock'].forEach(f=>{try{fs.rmSync(f,{force:true});}catch{}}); const ua=process.env.npm_config_user_agent||''; if(!ua.startsWith('pnpm/')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "build": "cross-env PORT=8080 BASE_PATH=/ pnpm run typecheck && cross-env PORT=8080 BASE_PATH=/ pnpm -r --if-present run build",
    "start": "cross-env NODE_ENV=production node --enable-source-maps artifacts/api-server/dist/index.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "cross-env": "^7.0.1",
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
